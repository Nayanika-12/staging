{
  "version": 2,
  "routes": [
    {
      "src": "/(.*)",
      "dest": "server/app.js"
    }
  ],
  "buildCommand": "npm install --prefix server && npm install --prefix client && npm run build --prefix client"
}